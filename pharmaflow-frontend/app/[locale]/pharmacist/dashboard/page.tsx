/**
 * Pharmacist dashboard — intentionally empty.
 * The PosShell renders the Metro tile start screen when the path is /pharmacist/dashboard.
 * This page exists only so the route resolves — PosShell handles the UI.
 */
export default function PharmacistDashboardPage() {
  return null;
}
